'use client';
import {useEffect,useState} from 'react';
import {createPortal} from 'react-dom';
export default function PostSortEnhancer(){const[asc,setAsc]=useState(false),[head,setHead]=useState<Element|null>(null);useEffect(()=>{const timer=setInterval(()=>{document.querySelectorAll('.ref-summary-tail').forEach((e:any)=>e.style.display='none');setHead(document.querySelector('.exact-posts .posts-head'));const container=document.querySelector('.post-view.list .ref-list')||document.querySelector('.post-view.cards .ref-card-grid');if(!container)return;const selector=container.classList.contains('ref-list')?'.ref-list-row':'.ref-card';const nodes=[...container.querySelectorAll(selector)] as HTMLElement[];nodes.sort((a,b)=>(asc?1:-1)*(dateOf(a)-dateOf(b)));nodes.forEach(n=>container.appendChild(n))},700);return()=>clearInterval(timer)},[asc]);if(!head)return null;return createPortal(<button className="post-sort-button" onClick={()=>setAsc(v=>!v)}>↕ {asc?'MAIS ANTIGAS PRIMEIRO':'MAIS RECENTES PRIMEIRO'}</button>,head)}
function dateOf(el:HTMLElement){const txt=[...el.querySelectorAll('small')].map(x=>x.textContent||'').find(x=>/\d{1,2}\/\d{1,2}\/\d{4}/.test(x))||'';const m=txt.match(/(\d{1,2})\/(\d{1,2})\/(\d{4})/);return m?new Date(Number(m[3]),Number(m[2])-1,Number(m[1])).getTime():0}
