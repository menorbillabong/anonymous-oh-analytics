'use client';
import {useEffect,useRef} from 'react';
import {supabase} from '@/lib/supabase';
export default function SettingsPersistenceSync(){const uid=useRef(''),last=useRef('');useEffect(()=>{let stopped=false;supabase.auth.getSession().then(async({data})=>{const id=data.session?.user.id||'';if(!id||stopped)return;uid.current=id;const{data:s}=await supabase.from('user_settings').select('monthly_post_goal').eq('user_id',id).maybeSingle();const value=String(Math.max(1,Number(s?.monthly_post_goal||60)));localStorage.setItem(`aoh:monthly-goal:${id}`,value);last.current=value});const timer=setInterval(async()=>{const id=uid.current;if(!id)return;const value=String(Math.max(1,Number(localStorage.getItem(`aoh:monthly-goal:${id}`)||60)));if(value===last.current)return;last.current=value;await supabase.from('user_settings').update({monthly_post_goal:Number(value),updated_at:new Date().toISOString()}).eq('user_id',id)},600);return()=>{stopped=true;clearInterval(timer)}},[]);return null}
