'use client';
import {useEffect,useState} from 'react';
import {createPortal} from 'react-dom';
import './add-publication-reference.css';
export default function ReferencePreviewInjector(){const[target,setTarget]=useState<HTMLElement|null>(null),[url,setUrl]=useState(''),[data,setData]=useState<any>(null),[loading,setLoading]=useState(false);useEffect(()=>{let cleanup=()=>{};const bind=()=>{cleanup();const screen=document.querySelector('.add-publication-screen') as HTMLElement|null;if(!screen){setTarget(null);return}screen.classList.add('reference-exact');const input=screen.querySelector('input[placeholder*="x.com"]') as HTMLInputElement|null;if(!input)return;const label=input.closest('label');if(!label)return;let host=screen.querySelector('.x-reference-preview-host') as HTMLElement|null;if(!host){host=document.createElement('div');host.className='x-reference-preview-host';label.insertAdjacentElement('afterend',host)}setTarget(host);setUrl(input.value);const f=()=>setUrl(input.value);input.addEventListener('input',f);cleanup=()=>input.removeEventListener('input',f)};bind();const o=new MutationObserver(bind);o.observe(document.body,{childList:true,subtree:true});return()=>{cleanup();o.disconnect()}},[]);useEffect(()=>{if(!/https?:\/\/(?:www\.)?(?:x\.com|twitter\.com)\/[^/]+\/status\/\d+/i.test(url)){setData(null);return}let live=true;const t=setTimeout(async()=>{setLoading(true);try{const r=await fetch('/api/x-metrics',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({url})});const j=await r.json();if(live&&r.ok&&!j.error)setData(j)}finally{if(live)setLoading(false)}},350);return()=>{live=false;clearTimeout(t)}},[url]);if(!target)return null;return createPortal(<>{loading&&<div className="x-reference-preview loading">Carregando publicação do X...</div>}{data&&!loading&&<Preview data={data} url={url}/>}</>,target)}
function Preview({data,url}:{data:any;url:string}){const imgs=(data.image_urls||[]).filter(Boolean),count=imgs.length+(data.video_url?1:0),d=data.published_at?new Date(data.published_at):data.published_date?new Date(data.published_date+'T12:00:00'):null,date=d&&!Number.isNaN(d.getTime())?d.toLocaleString('pt-BR',{day:'2-digit',month:'long',year:'numeric',hour:'2-digit',minute:'2-digit'}).replace(' às',','):'';return <div className="x-reference-preview"><div className="x-preview-head">{data.author_avatar?<img className="x-preview-avatar" src={data.author_avatar} alt="Avatar"/>:<div className="x-preview-avatar x-preview-avatar-fallback">𝕏</div>}<div className="x-preview-author"><strong>{data.author_name||data.author_handle}</strong><span>@{data.author_handle}</span></div><div className="x-preview-logo">𝕏</div></div><div className="x-preview-text">{data.title}</div>{count>0&&<div className={`x-preview-media ${count===1?'one':''}`}>{imgs.map((src:string)=><img key={src} src={src} alt="Mídia"/>)}{data.video_url&&<video src={data.video_url} controls playsInline muted/>}</div>}<div className="x-preview-stats"><span>◉ {Number(data.views||0).toLocaleString('pt-BR')}</span><span>◯ {Number(data.comments||0).toLocaleString('pt-BR')}</span><span>↻ {Number(data.reposts||0).toLocaleString('pt-BR')}</span><span className="heart">♥ {Number(data.likes||0).toLocaleString('pt-BR')}</span></div><div className="x-preview-foot"><span>{date}</span><a href={url} target="_blank" rel="noopener noreferrer">Ver no X ↗</a></div></div>}
