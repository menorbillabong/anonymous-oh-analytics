import {gunzipSync} from 'node:zlib';import {mkdirSync,readFileSync,writeFileSync} from 'node:fs';import {dirname} from 'node:path';const parts=Array.from({length:10},(_,i)=>readFileSync(new URL(`./restore-parts/part${String(i+1).padStart(2,'0')}.txt`,import.meta.url),'utf8')).join('');const files=JSON.parse(gunzipSync(Buffer.from(parts,'base64')).toString('utf8'));for(const [path,content] of Object.entries(files)){mkdirSync(dirname(path),{recursive:true});writeFileSync(path,content,'utf8')}